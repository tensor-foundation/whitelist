/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/web3.js';
import { findAuthorityPda } from '../pdas';
import { TENSOR_WHITELIST_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export type UnfreezeWhitelistInstruction<
  TProgram extends string = typeof TENSOR_WHITELIST_PROGRAM_ADDRESS,
  TAccountWhitelist extends string | IAccountMeta<string> = string,
  TAccountWhitelistAuthority extends string | IAccountMeta<string> = string,
  TAccountOwner extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountWhitelist extends string
        ? WritableAccount<TAccountWhitelist>
        : TAccountWhitelist,
      TAccountWhitelistAuthority extends string
        ? ReadonlyAccount<TAccountWhitelistAuthority>
        : TAccountWhitelistAuthority,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            IAccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UnfreezeWhitelistInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type UnfreezeWhitelistInstructionDataArgs = {};

export function getUnfreezeWhitelistInstructionDataEncoder(): Encoder<UnfreezeWhitelistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([215, 119, 9, 92, 160, 139, 226, 253]),
    })
  );
}

export function getUnfreezeWhitelistInstructionDataDecoder(): Decoder<UnfreezeWhitelistInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUnfreezeWhitelistInstructionDataCodec(): Codec<
  UnfreezeWhitelistInstructionDataArgs,
  UnfreezeWhitelistInstructionData
> {
  return combineCodec(
    getUnfreezeWhitelistInstructionDataEncoder(),
    getUnfreezeWhitelistInstructionDataDecoder()
  );
}

export type UnfreezeWhitelistAsyncInput<
  TAccountWhitelist extends string = string,
  TAccountWhitelistAuthority extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  whitelist: Address<TAccountWhitelist>;
  /**
   * there can only be 1 whitelist authority (due to seeds),
   * and we're checking that 1)the correct cosigner is present on it, and 2)is a signer
   */
  whitelistAuthority?: Address<TAccountWhitelistAuthority>;
  /** unfreezing requires owner */
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getUnfreezeWhitelistInstructionAsync<
  TAccountWhitelist extends string,
  TAccountWhitelistAuthority extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
>(
  input: UnfreezeWhitelistAsyncInput<
    TAccountWhitelist,
    TAccountWhitelistAuthority,
    TAccountOwner,
    TAccountSystemProgram
  >
): Promise<
  UnfreezeWhitelistInstruction<
    typeof TENSOR_WHITELIST_PROGRAM_ADDRESS,
    TAccountWhitelist,
    TAccountWhitelistAuthority,
    TAccountOwner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = TENSOR_WHITELIST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    whitelist: { value: input.whitelist ?? null, isWritable: true },
    whitelistAuthority: {
      value: input.whitelistAuthority ?? null,
      isWritable: false,
    },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.whitelistAuthority.value) {
    accounts.whitelistAuthority.value = await findAuthorityPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.whitelist),
      getAccountMeta(accounts.whitelistAuthority),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getUnfreezeWhitelistInstructionDataEncoder().encode({}),
  } as UnfreezeWhitelistInstruction<
    typeof TENSOR_WHITELIST_PROGRAM_ADDRESS,
    TAccountWhitelist,
    TAccountWhitelistAuthority,
    TAccountOwner,
    TAccountSystemProgram
  >;

  return instruction;
}

export type UnfreezeWhitelistInput<
  TAccountWhitelist extends string = string,
  TAccountWhitelistAuthority extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  whitelist: Address<TAccountWhitelist>;
  /**
   * there can only be 1 whitelist authority (due to seeds),
   * and we're checking that 1)the correct cosigner is present on it, and 2)is a signer
   */
  whitelistAuthority: Address<TAccountWhitelistAuthority>;
  /** unfreezing requires owner */
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getUnfreezeWhitelistInstruction<
  TAccountWhitelist extends string,
  TAccountWhitelistAuthority extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
>(
  input: UnfreezeWhitelistInput<
    TAccountWhitelist,
    TAccountWhitelistAuthority,
    TAccountOwner,
    TAccountSystemProgram
  >
): UnfreezeWhitelistInstruction<
  typeof TENSOR_WHITELIST_PROGRAM_ADDRESS,
  TAccountWhitelist,
  TAccountWhitelistAuthority,
  TAccountOwner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = TENSOR_WHITELIST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    whitelist: { value: input.whitelist ?? null, isWritable: true },
    whitelistAuthority: {
      value: input.whitelistAuthority ?? null,
      isWritable: false,
    },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.whitelist),
      getAccountMeta(accounts.whitelistAuthority),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getUnfreezeWhitelistInstructionDataEncoder().encode({}),
  } as UnfreezeWhitelistInstruction<
    typeof TENSOR_WHITELIST_PROGRAM_ADDRESS,
    TAccountWhitelist,
    TAccountWhitelistAuthority,
    TAccountOwner,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedUnfreezeWhitelistInstruction<
  TProgram extends string = typeof TENSOR_WHITELIST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    whitelist: TAccountMetas[0];
    /**
     * there can only be 1 whitelist authority (due to seeds),
     * and we're checking that 1)the correct cosigner is present on it, and 2)is a signer
     */

    whitelistAuthority: TAccountMetas[1];
    /** unfreezing requires owner */
    owner: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: UnfreezeWhitelistInstructionData;
};

export function parseUnfreezeWhitelistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedUnfreezeWhitelistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      whitelist: getNextAccount(),
      whitelistAuthority: getNextAccount(),
      owner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUnfreezeWhitelistInstructionDataDecoder().decode(instruction.data),
  };
}
