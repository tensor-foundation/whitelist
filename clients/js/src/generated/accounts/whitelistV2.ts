/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  EncodedAccount,
  FetchAccountConfig,
  FetchAccountsConfig,
  MaybeAccount,
  MaybeEncodedAccount,
  assertAccountExists,
  assertAccountsExist,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
} from '@solana/accounts';
import {
  Address,
  getAddressDecoder,
  getAddressEncoder,
} from '@solana/addresses';
import {
  Codec,
  Decoder,
  Encoder,
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  mapEncoder,
} from '@solana/codecs';
import { WhitelistV2Seeds, findWhitelistV2Pda } from '../pdas';
import {
  Condition,
  ConditionArgs,
  State,
  StateArgs,
  getConditionDecoder,
  getConditionEncoder,
  getStateDecoder,
  getStateEncoder,
} from '../types';

export type WhitelistV2<TAddress extends string = string> = Account<
  WhitelistV2AccountData,
  TAddress
>;

export type MaybeWhitelistV2<TAddress extends string = string> = MaybeAccount<
  WhitelistV2AccountData,
  TAddress
>;

export type WhitelistV2AccountData = {
  discriminator: Array<number>;
  version: number;
  bump: number;
  uuid: Uint8Array;
  state: State;
  updateAuthority: Address;
  namespace: Address;
  freezeAuthority: Address;
  conditions: Array<Condition>;
};

export type WhitelistV2AccountDataArgs = {
  version: number;
  bump: number;
  uuid: Uint8Array;
  state: StateArgs;
  updateAuthority: Address;
  namespace: Address;
  freezeAuthority: Address;
  conditions: Array<ConditionArgs>;
};

export function getWhitelistV2AccountDataEncoder(): Encoder<WhitelistV2AccountDataArgs> {
  return mapEncoder(
    getStructEncoder([
      ['discriminator', getArrayEncoder(getU8Encoder(), { size: 8 })],
      ['version', getU8Encoder()],
      ['bump', getU8Encoder()],
      ['uuid', getBytesEncoder({ size: 32 })],
      ['state', getStateEncoder()],
      ['updateAuthority', getAddressEncoder()],
      ['namespace', getAddressEncoder()],
      ['freezeAuthority', getAddressEncoder()],
      ['conditions', getArrayEncoder(getConditionEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: [136, 184, 45, 191, 85, 203, 191, 119],
    })
  );
}

export function getWhitelistV2AccountDataDecoder(): Decoder<WhitelistV2AccountData> {
  return getStructDecoder([
    ['discriminator', getArrayDecoder(getU8Decoder(), { size: 8 })],
    ['version', getU8Decoder()],
    ['bump', getU8Decoder()],
    ['uuid', getBytesDecoder({ size: 32 })],
    ['state', getStateDecoder()],
    ['updateAuthority', getAddressDecoder()],
    ['namespace', getAddressDecoder()],
    ['freezeAuthority', getAddressDecoder()],
    ['conditions', getArrayDecoder(getConditionDecoder())],
  ]);
}

export function getWhitelistV2AccountDataCodec(): Codec<
  WhitelistV2AccountDataArgs,
  WhitelistV2AccountData
> {
  return combineCodec(
    getWhitelistV2AccountDataEncoder(),
    getWhitelistV2AccountDataDecoder()
  );
}

export function decodeWhitelistV2<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): WhitelistV2<TAddress>;
export function decodeWhitelistV2<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeWhitelistV2<TAddress>;
export function decodeWhitelistV2<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): WhitelistV2<TAddress> | MaybeWhitelistV2<TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getWhitelistV2AccountDataDecoder()
  );
}

export async function fetchWhitelistV2<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<WhitelistV2<TAddress>> {
  const maybeAccount = await fetchMaybeWhitelistV2(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeWhitelistV2<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeWhitelistV2<TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeWhitelistV2(maybeAccount);
}

export async function fetchAllWhitelistV2(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<WhitelistV2[]> {
  const maybeAccounts = await fetchAllMaybeWhitelistV2(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeWhitelistV2(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeWhitelistV2[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeWhitelistV2(maybeAccount));
}

export function getWhitelistV2Size(): number {
  return 239;
}

export async function fetchWhitelistV2FromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: WhitelistV2Seeds,
  config: FetchAccountConfig & { programAddress?: Address } = {}
): Promise<WhitelistV2> {
  const maybeAccount = await fetchMaybeWhitelistV2FromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeWhitelistV2FromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: WhitelistV2Seeds,
  config: FetchAccountConfig & { programAddress?: Address } = {}
): Promise<MaybeWhitelistV2> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findWhitelistV2Pda(seeds, { programAddress });
  return fetchMaybeWhitelistV2(rpc, address, fetchConfig);
}
